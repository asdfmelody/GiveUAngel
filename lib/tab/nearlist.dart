import 'package:flutter/material.dart';

class NearlistPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Nearlist Page'),
      ),
    );
  }
}
